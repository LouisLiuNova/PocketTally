# 账本遗留问题业务决策记录

本文保留账本遗留问题的历史讨论、实施背景和验收思路。待办事项已经归并到 `TODO.md`，是否仍需实施以及优先级均以 `TODO.md` 为准。

> [!WARNING]
> 本文不是待办清单，也可能包含已完成或已被后续决策取代的内容。交易类型封闭集合、有限正金额、`occurred_at`、账户 PATCH 禁止修改余额等当前约束，应以代码、契约和 `TODO.md` 为准。

## 已被当前简化决策取代的历史方案

2026-09-03 已确认 PocketTally 当前按个人、单用户、本地 SQLite、单应用写入设计。以下方案不再属于当前实现路线：

- 让 SQLite 触发器重复实现分类用途、退款累计和关系循环等跨表业务规则；
- 将手工 SQL 或第三方程序直接写数据库作为受支持业务路径；
- 要求 HTTP Schema、服务层、ORM 和 SQLite 分别完整实现同一组业务规则；
- 在尚无已发布数据库时实施版本化迁移、历史数据升级和回滚演练；
- 为尚不存在的多用户、多进程写入或高并发退款预先设计协调机制。

当前职责边界是：HTTP/Pydantic 校验请求形状，服务层执行完整业务规则，SQLite 保留结构约束。已有余额同步触发器是待移除的现状，不代表后续业务规则继续使用触发器实现。最新实施顺序以 `TODO.md` 为准。

对应 GitHub 整理结果：原 Issue #6 已由精简的支出退款 Issue #10 替代并关闭；版本化迁移 Issue #9 已按当前不计划关闭。Issue #2 和 #5 保留为分类结构与用途的待决策项，Issue #11 负责最小运行时数据库层和余额职责收敛，Issue #12 负责基础资源 API，Issue #4 在这些前置工作后完成交易写入校验。

## P1：交易的类型与资金流向规则（历史方案）

### 历史讨论形成的方案

旧版方案围绕以下三类交易讨论资金流向：

- `income`：收入；
- `expense`：支出；
- `transfer`：转账。

> [!NOTE]
> 当前枚举还包含 `balance_adjustment`，数据库和契约保证交易金额为有限正数；四类交易的账户组合以及 `balance_adjustment` 的资金流向仍需按 `TODO.md` 完成，不能把下表视为已经生效的约束。

旧版方案建议让 `amount` 表示交易的绝对金额并要求有限正数，账户余额的增减由交易类型以及来源、目标账户共同确定。

| 交易类型 | `src_account_id` | `dest_account_id` | 对账户余额的影响 | 分类 |
| --- | --- | --- | --- | --- |
| `income` | 必须为空 | 必填 | 目标账户增加 `amount` | 必填，且必须是收入分类 |
| `expense` | 必填 | 必须为空 | 来源账户减少 `amount` | 必填，且必须是支出分类 |
| `transfer` | 必填 | 必填，且必须与来源账户不同 | 来源账户减少 `amount`，目标账户增加 `amount` | 必须为空 |

数据库字段继续使用现有的 `src_account_id` 和 `dest_account_id` 命名；HTTP 契约对应使用 `sourceAccountId` 和 `destinationAccountId`。

> [!IMPORTANT]
> 转账只改变账户间的资金分布，不构成收入或支出，也不进入收支分类统计。

### 分类用途待确认

为落实“分类与交易类型匹配”，分类需要增加用途字段。建议采用封闭集合：

- `income`：只能被收入交易引用；
- `expense`：只能被支出交易引用。

建议暂不提供可同时用于收入和支出的 `both` 类型，并要求父子分类用途一致，避免分类树聚合和收支报表出现语义冲突。转账不得引用分类。

待确认决策：**分类是否严格分为 `income` 和 `expense`，并要求父子分类用途一致。**

## 完整性约束分层

> [!WARNING]
> 本节记录早期“所有写入路径均由数据库保护”的方案。该目标已被上方的 2026-09-03 简化决策取代；以下 SQLite 跨表触发器和直接 SQL 验收内容仅作历史参考。

验收目标是：任意写入路径都不能创建未知类型、非正数或非有限金额、同账户转账、账户组合不合法，或者分类与交易类型不匹配的交易。

### HTTP 契约层

- 将交易 `type` 从任意非空字符串改为 `income`、`expense`、`transfer` 枚举。
- 将 `amount` 限制为有限且严格大于零的数值。
- 将 `source_account_id` 和 `category_id` 调整为可空字段，以表达收入和转账的合法字段组合。
- 创建请求执行交易类型、账户组合和分类是否存在等基础校验。
- 返回稳定、清晰的字段级或业务级错误信息。

HTTP 契约层负责尽早拒绝非法请求，但不能作为唯一的完整性边界。

### 服务层

- 建立唯一、可复用的完整交易校验入口，供 API、导入任务和未来后台任务共同使用。
- 创建交易时校验完整候选状态。
- 部分更新交易时，先读取现有交易，再与 PATCH 字段合并，最后校验合并后的完整状态。
- 查询分类用途并验证其与交易类型匹配。
- 在同一个数据库事务中完成校验和写入，避免部分状态落库。

> [!WARNING]
> 部分更新不能只校验请求体。例如仅把现有支出交易的 `type` 改为 `income`，必须结合原有账户和分类字段重新校验，否则会形成非法组合。

### SQLite 层

在 `transactions` 表中增加数据库级约束：

- `type` 必须属于 `income`、`expense`、`transfer`；
- `amount > 0`；
- 收入必须仅有目标账户且具有分类；
- 支出必须仅有来源账户且具有分类；
- 转账必须同时具有不同的来源、目标账户且分类为空。

建议使用等价于以下逻辑的约束：

```sql
CHECK (type IN ('income', 'expense', 'transfer')),
CHECK (amount > 0),
CHECK (
    (type = 'income'
        AND src_account_id IS NULL
        AND dest_account_id IS NOT NULL
        AND category IS NOT NULL)
    OR
    (type = 'expense'
        AND src_account_id IS NOT NULL
        AND dest_account_id IS NULL
        AND category IS NOT NULL)
    OR
    (type = 'transfer'
        AND src_account_id IS NOT NULL
        AND dest_account_id IS NOT NULL
        AND src_account_id <> dest_account_id
        AND category IS NULL)
)
```

分类用途匹配涉及 `categories` 表，不能只依赖单表 `CHECK`，需要增加：

- 交易 `BEFORE INSERT` 触发器；
- 交易相关字段的 `BEFORE UPDATE` 触发器；
- 分类用途变更保护，防止更新分类后使已有交易变为非法状态。

同一组约束必须同时体现在 `db/schema.sql` 和 SQLModel 元数据中，确保直接执行 DDL 与 `SQLModel.metadata.create_all()` 的建库结果一致。

> [!NOTE]
> `amount > 0` 可以拒绝零和负数；API 与服务层仍需显式拒绝 `NaN` 和正负无穷。长期应按 `TODO.md` 的金额精度事项迁移到最小货币单位整数。

## 验收测试矩阵

服务层和直接 SQL 写入至少覆盖以下场景：

- 拒绝未知交易类型；
- 拒绝零、负数、`NaN` 和正负无穷金额；
- 收入只接受目标账户；
- 支出只接受来源账户；
- 转账必须同时具有来源、目标账户；
- 拒绝来源和目标相同的转账；
- 收入拒绝支出分类；
- 支出拒绝收入分类；
- 转账拒绝任何分类；
- 部分更新类型后重新校验完整交易；
- 部分更新来源或目标账户后重新校验完整交易；
- 修改分类用途时，不得使已有交易与分类失配；
- 直接执行 SQL `INSERT` 或 `UPDATE` 也无法绕过完整性规则；
- 合法转账对所有账户合计余额的净影响为零。

最后一项需要与“余额权威来源与原子记账”任务共同完成。

## 推荐实施顺序

1. 确认分类用途以及父子分类一致性规则。
2. 给分类增加 `income`、`expense` 用途。
3. 更新 DBML、`schema.sql` 和 SQLModel，使两种建库方式具有相同约束。
4. 更新 Pydantic 模型、JSON Schema 和 OpenAPI 契约。
5. 建立统一的完整交易校验函数，并支持 PATCH 合并校验。
6. 增加数据库 `CHECK`、分类匹配触发器及直接 SQL 回归测试。
7. 引入版本化迁移，规范化已有交易的金额、账户字段和分类。
8. 继续处理“余额权威来源与原子记账”，实现账户余额变更。

> [!CAUTION]
> 迁移历史数据前，需要先识别现有负数支出、收入使用来源账户以及转账带分类等旧数据，并为无法自动判断的记录制定人工处理或审计策略。

## P1：余额权威来源与原子记账

### 已确认的业务决策与实现

旧版“暂不限制账户直接修改余额”的决策已被后续规则取代。当前规则是：账户 PATCH 不得修改余额，调账必须创建 `balance_adjustment` 类型交易，以保留完整审计轨迹。

- 新建账户余额固定为 0，不设置 `opening_balance` 或其他初始余额字段。
- `accounts.amount_minor` 表示由有效交易重建的当前余额投影，不是独立的账务事实。
- 收入、支出、转账和余额调整均按资金方向生成余额分录；作废交易保留记录但不参与余额计算。
- 余额调整请求必须带有 `increase` 或 `decrease` 方向，并写入一笔真实的 `balance_adjustment` 交易。
- 账本服务不在内部提交事务，交易写入、余额重算和失败回滚由同一个数据库事务边界控制。
- SQLite DDL 在交易新增、更新、删除后重建余额投影；直接改写余额列也会被重建结果覆盖。

> [!IMPORTANT]
> 账户 PATCH 修改余额已经禁止；余额调整必须创建 `balance_adjustment` 交易，不能直接改写账户余额。

### 与交易资金流向规则的边界

“交易的类型与资金流向规则”仍需按 `TODO.md` 实施。账本服务已经对传入的交易资金方向执行基础校验；交易类型、账户组合、分类用途和退款/冲正规则仍由各自待办负责。

转账已纳入余额重算：来源账户减少、目标账户增加，所有账户合计余额不变。

### 后续重新评估条件

出现以下任一业务需求时，应重新评估当前余额投影是否需要扩展：

- 支持待入账与已入账等交易状态；
- 需要复式分录、跨币种或更复杂的结算规则；
- 需要将余额缓存拆分为独立的对账或结算快照。

## P1：退款、冲正与已入账交易的修订

### 已确定的退款规则

- 一笔原交易可以对应一笔或多笔退款交易；
- 所有有效退款的累计金额不得超过原交易金额；
- 退款必须沿原交易的资金路径反向退回原账户，不允许在退款时指定其他账户；
- 退款金额继续遵守全局金额规则，使用有限正数，由退款关系和反向账户字段表达资金方向。

“退回原账户”按原交易类型具体解释为：

| 原交易 | 退款账户规则 |
| --- | --- |
| 支出 | 退款只能进入原交易的 `src_account_id` |
| 收入 | 若允许退回收入，资金只能从原交易的 `dest_account_id` 退回 |
| 转账 | 不作为普通退款处理；如需撤销，应使用冲正语义 |

> [!IMPORTANT]
> “收入退款”是否属于当前产品范围尚未确定。在确定前，建议普通退款仅支持原支出交易，拒绝对收入和转账创建退款。

累计金额校验必须排除已经作废或被冲正的退款。当前由服务层在同一个数据库事务中读取有效退款合计并写入新退款；SQLite 不再重复实现跨表退款业务规则。若未来出现多进程写入，再重新评估并发协调和数据库保护。

### 退款与分类统计

支出退款在资金方向上表现为流入，但业务报表应冲减原支出，而不是产生一笔普通收入。因此，退款应沿用原交易的支出分类，并在支出报表中以抵减金额计算。

这构成“收入必须使用收入分类”规则的受控例外：

- 普通 `income` 必须引用收入分类；
- 原支出的退款虽然资金流向账户，但必须引用原交易的支出分类；
- 退款分类不能由客户端任意指定，应由服务端从原交易复制；
- 转账冲正仍不使用分类。

数据库与服务层的分类匹配校验必须显式识别退款关系，不能只根据资金流向类型判断分类用途。

### `is_refund` 与关系字段评估

建议移除 `is_refund`。布尔标记与 `related_transaction_id` 可以形成以下不一致状态：

- `is_refund = true`，但没有关联原交易；
- `is_refund = false`，但关联了原交易；
- 关联目标并非允许退款的交易；
- 同一个通用关联字段无法说明退款、冲正或其他关系。

不建议仅以 `reversal_of_transaction_id` 替代 `is_refund`，因为 `reversal` 表示完整冲销，而已经确定支持累计退款意味着可能存在部分退款和多次退款。把部分退款记录为 `reversal` 会造成错误的领域语义。

推荐拆分为两个具有明确语义的可空关系字段：

- `refund_of_transaction_id`：表示全额、部分或多次退款；
- `reversal_of_transaction_id`：表示对原交易进行一次完整冲正。

一笔交易最多只能设置其中一个关系字段。是否属于退款或冲正由关系字段本身确定，不再保存可漂移的布尔标记。

如果当前阶段尚不实施冲正，可先只引入 `refund_of_transaction_id`，并在冲正业务规则确定后再增加 `reversal_of_transaction_id`；不要继续使用含义模糊的 `related_transaction_id` 承载多种业务关系。

### 冲正的建议语义

以下内容是推荐方案，尚未形成最终业务决策：

- 冲正必须完整抵消一笔原交易，金额必须等于原交易金额；
- 每笔原交易最多存在一笔有效冲正；
- 冲正必须使用与原交易完全相反的账户方向；
- 原交易已经存在退款时，不能直接按原金额冲正，应先处理已有退款或拒绝冲正；
- 冲正与原交易都保留，不物理删除，以维持审计链；
- 不允许冲正交易再次作为普通退款或冲正的原交易，避免形成关系链和循环。

### 已入账交易修订待确认

当前尚未决定已入账交易是否允许原地修改。建议采用以下长期规则：

- 未入账交易可以直接编辑；
- 已入账交易不原地修改金额、账户、类型、分类和发生时间；
- 业务错误通过“冲正原交易并创建更正交易”处理；
- 描述、标签等不影响账务结果的元数据是否允许修改，可单独决定；
- 原交易、冲正交易和更正交易之间应保留可审计关系。

该方案依赖尚未建立的交易生命周期与入账状态，因此本阶段只能记录方向，不能完整实施“已入账不可修改”。

### 待确认决策

1. 普通退款是否只支持原支出交易。
2. 是否采纳 `refund_of_transaction_id` 与 `reversal_of_transaction_id` 分离的关系设计。
3. 是否采纳“一笔等额反向交易且每笔原交易最多一次”的冲正定义。
4. 已入账交易是否禁止原地修改账务字段，统一采用冲正加更正交易。
