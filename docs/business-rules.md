# PocketTally 业务规则

本文档定义 PocketTally 当前账本领域的业务规则，并集中登记尚未决策的业务问题。业务规则描述“系统应当如何工作”，具体实施任务和优先级仍由 [`TODO.md`](../TODO.md) 管理。

> [!IMPORTANT]
> 本文档区分“已生效”“已确认但未完全落地”和“待决策”。只有前两类属于已确认的产品语义；待决策内容不得作为开发默认值或验收依据。

## 1. 文档约定

### 1.1 规则状态

| 状态 | 含义 |
| --- | --- |
| 已生效 | 业务含义已确认，且当前主要实现层已经执行该规则。 |
| 部分落地 | 业务含义已确认，但 HTTP 契约、服务、ORM、DDL、导入或测试之间仍存在缺口。 |
| 待决策 | 尚未形成产品决策，不属于当前规范。 |
| MVP 后续 | 已识别但明确不进入当前 MVP。 |

### 1.2 事实来源与优先级

发生描述冲突时，按以下顺序处理：

1. 本文档中的已确认业务规则；
2. `TODO.md` 中尚未完成的实施范围和验收要求；
3. 当前 HTTP Schema、账本服务、ORM、SQLite DDL 与自动化测试；
4. `TODO-IMP.md` 中的历史讨论和推荐方案。

`TODO-IMP.md` 不具有规范性，其中与本文档不一致的内容均视为历史背景。

### 1.3 当前产品与运行边界

**状态：已确认｜决定日期：2026-09-03**

- PocketTally 当前面向个人、单用户和本地 SQLite 使用场景。
- 只有 PocketTally 应用可以写入数据库；未来导入功能也必须调用同一业务服务。
- 手工 SQL 和第三方程序直接写数据库不属于受支持能力。
- 当前仍处于开发阶段，没有需要兼容的已发布数据库；版本化迁移、历史升级和回滚演练延后到首次发布前重新评估。
- 多用户、分布式写入、外部数据库写入和高并发控制不得作为当前设计的默认前提。

### 1.4 完整性职责

**状态：已确认但待完全落地**

| 层次 | 当前职责 |
| --- | --- |
| HTTP / Pydantic | 校验类型、格式、必填性和可以仅依据请求体判断的字段组合，并返回稳定错误。 |
| 业务服务 | 执行交易完整状态、分类、退款、余额和关系等跨字段、跨记录规则；创建、PATCH 和未来导入共用同一入口。 |
| SQLite | 保留主键、外键、唯一性、非空、封闭枚举、正金额及适合单行表达的 `CHECK`。 |

SQLite 不再承担分类用途、关系循环、退款累计等跨表业务规则，也不承诺直接 SQL 写入后的业务状态合法。应用启动时仍必须为每个连接启用外键约束。

## 2. 产品与金额口径

### BR-MON-001：币种与金额精度

**状态：已生效**

- 当前系统只处理人民币 CNY，不在账户、交易或 HTTP 契约中保存币种字段。
- HTTP 金额以十进制元表达；服务端收到金额后立即转换为 `Decimal`。
- 金额统一使用 `ROUND_HALF_UP` 舍入到分，并以整数分 `amount_minor` 持久化和计算。
- 交易金额按舍入后的结果判断，必须严格大于 0。
- `NaN`、正负无穷和其他非有限数值不是合法金额。

### BR-MON-002：金额方向

**状态：已生效**

- 交易金额始终保存为正数。
- 资金增加或减少由交易类型、来源账户、目标账户和余额调整方向共同表达，不使用负交易金额表达支出或退款。

## 3. 账户规则

### BR-ACC-001：账户类型

**状态：已生效**

- 账户类型为封闭集合：`debit`、`credit`。
- `debit` 表示借记账户，余额不得小于 0。
- `credit` 表示信用账户，允许出现负余额。

### BR-ACC-002：账户创建与余额权威来源

**状态：已生效**

- 新建账户余额固定为 0，不提供初始余额字段。
- 账户余额是所有有效交易计算得到的可重建投影，不是可独立修改的账务事实。
- 账户创建和账户 PATCH 均不得接受余额字段。
- 没有有效交易的账户余额为 0。

### BR-ACC-003：余额调整

**状态：已生效**

- 人工调账必须创建 `balance_adjustment` 交易，不能直接改写账户余额。
- 余额调整只影响一个账户，使用 `increase` 或 `decrease` 明确资金方向。
- 余额调整不得指定目标账户和分类。
- 余额调整与普通交易一样保留发生时间和审计记录，也可以被作废。

### BR-ACC-004：名称唯一性

**状态：已生效**

- 账户名称在当前数据域内全局唯一。
- 唯一性按 SQLite `NOCASE` 规则比较，即 ASCII 大小写不敏感。

## 4. 交易规则

### BR-TXN-001：交易类型

**状态：已生效**

交易类型为封闭集合：

| 类型 | 含义 |
| --- | --- |
| `income` | 外部资金进入一个账户。 |
| `expense` | 资金从一个账户流出。 |
| `transfer` | 资金在两个账户之间转移。 |
| `balance_adjustment` | 对一个账户执行有审计记录的余额调整。 |

当前代码与契约已实现以上四类基础交易。支出退款已确认使用独立的
`expense_refund` 类型，但在 Issue #10 完成前不得将其标记为已生效，也不得用
普通 `income` 交易代替。

### BR-TXN-002：账户路由

**状态：部分落地**

| 交易类型 | 来源账户 | 目标账户 | 余额影响 |
| --- | --- | --- | --- |
| `income` | 必须为空 | 必填 | 目标账户增加交易金额。 |
| `expense` | 必填 | 必须为空 | 来源账户减少交易金额。 |
| `transfer` | 必填 | 必填，且与来源账户不同 | 来源账户减少、目标账户增加相同金额。 |
| `balance_adjustment` | 必填，表示受影响账户 | 必须为空 | 按 `increase` 或 `decrease` 增减余额。 |

- 交易引用的账户必须存在。
- 转账对所有账户余额合计的净影响为 0。
- 路由校验应基于交易完整状态执行；PATCH 必须先合并原记录和请求字段，再校验结果。

> [!WARNING]
> 当前账本服务已执行账户路由校验，但创建/PATCH Schema 尚未正确表达全部组合，服务层也未校验分类用途；导入路径尚未建立。SQLite 只需保留适合单行表达的结构约束。完成标准见 `TODO.md` 的“核心账本规则”。

### BR-TXN-003：原子记账

**状态：已生效**

- 交易写入、受影响账户余额重算和失败回滚必须位于同一个数据库事务中。
- 账本服务不在内部提前提交事务，事务的提交或回滚由调用方控制。
- 任一步骤失败时，不得留下交易已写入但余额未同步的中间状态。
- `accounts.amount_minor` 暂时保留为服务层维护的可重建投影；当前 SQLite 跨表余额触发器将在实现收敛阶段移除，避免数据库与 Python 重复执行同一业务流程。

### BR-TXN-004：有效交易与作废

**状态：部分落地**

- `voided_at` 为空的交易为有效交易，参与余额计算。
- 作废交易保留原记录和作废时间，但不再参与余额计算。
- 已作废交易不能通过账本服务直接修改，也不能重复作废。
- 当前只定义了作废对余额的影响；谁可以作废、允许作废的状态和时间限制仍待生命周期决策。

### BR-TXN-005：时间字段

**状态：部分落地**

- `occurred_at` 表示交易实际发生时间，由客户端在创建交易时提供。
- `created_at` 表示服务器录入时间，由服务器生成。
- `updated_at` 表示记录或标签关系最近更新时间，由服务器维护。
- `voided_at` 表示交易作废时间；为空表示尚未作废。
- “入账时间”和完整交易状态尚未定义，不能用 `created_at` 或 `updated_at` 替代。

### BR-TXN-006：交易修订

**状态：部分落地**

- 当前账本服务允许修改未作废交易，并在同一事务中重算余额。
- 修改后的交易仍必须满足金额、类型和账户路由规则。
- 是否允许已入账交易原地修改账务字段尚未决策；当前行为不代表长期产品规则。

## 5. 分类与标签

### BR-CAT-001：分类结构

**状态：部分落地**

- MVP 保留树状分类。分类可以是顶级分类，也可以通过 `parent_category_id` 指向一个父分类。
- 分类名称在当前数据域内全局唯一，并按 SQLite `NOCASE` 规则比较。
- 当前没有分类用途字段，也没有形成收入/支出用途和父子用途一致性规则。
- 创建和移动分类时必须拒绝自身引用和多节点循环；该规则由 Issue #2 实现。
- 父分类被子分类引用时不能物理删除。分类树最大深度当前不设产品限制，但实现必须使用可终止的遍历并防御已有异常关系。

### BR-TAG-001：交易标签

**状态：已生效**

- 标签是可由多笔交易复用的独立实体。
- 一笔交易不能重复关联同一标签。
- 标签名称在当前数据域内全局唯一，并按 SQLite `NOCASE` 规则比较。
- 增删交易标签关联应更新交易的 `updated_at`。

### BR-RES-001：基础资源删除

**状态：已确认但待完全落地**

- 账户、分类和标签只有在未被交易、子分类或关联记录引用时才允许物理删除。
- 被引用资源的删除请求返回稳定的 `409 Conflict`，不得级联删除交易，也不得为了删除资源而静默清空历史关系。
- 成功删除返回 `204 No Content`，且不返回响应体。
- 归档与物理删除是不同能力；未来需要隐藏已停用资源时，应单独设计 `archived_at` 或等价状态。

## 6. 已确认但尚未实现完整的业务规则

以下规则已经形成业务方向，但仍需要设计和实现，不应标记为已生效。

### BR-REF-001：支出退款

**状态：已确认但待完全落地｜决定日期：2026-09-03**

- 支出退款使用独立的 `expense_refund` 交易类型，不复用普通 `income` 类型。
- 退款必须关联一笔有效的原交易。
- 退款不得关联自身，退款关系不得形成循环。
- 一笔原交易可以有一笔或多笔退款；所有有效退款累计金额不得超过原交易金额。
- 退款必须沿原交易资金路径反向退回，不允许由客户端选择其他账户。
- 已作废或已被冲正的退款不计入有效退款累计金额。
- 支出退款在报表中冲减原支出，不记为普通收入，并沿用原支出分类。
- 客户端只提交 `refundOfTransactionId`、金额、发生时间和可选说明；退款账户和分类由服务端从原支出复制，客户端不能覆盖。
- `expense_refund` 不通过普通交易创建或 PATCH 接口伪造，只能经过退款业务入口创建。

> [!WARNING]
> 当前代码仍使用 `is_refund` 与 `related_transaction_id`，尚未实现
> `expense_refund`、`refund_of_transaction_id`、累计金额和报表规则。Issue #10
> 必须同步替换这些旧字段。当前单应用写入场景不单独引入高并发协调机制。

### BR-RPT-001：转账报表口径

**状态：部分落地**

- 转账只改变账户之间的资金分布，不构成收入或支出。
- 转账不得进入收入、支出或对应分类的统计。
- 当前尚未建立完整报表实现和回归测试。

## 7. 待决策业务规则

本节是业务决策登记表。每项决策完成后，应记录结论和日期，将确定的规则移入前述章节，并更新 `TODO.md` 的实施状态。

### DEC-CAT-001：分类用途与父子一致性

**状态：待决策｜关联 TODO：P1 核心账本规则**

分类结构已决定保留树状层级；仍需要决定：

1. 分类是否严格分为 `income` 和 `expense`；
2. 是否允许同时用于收入和支出的通用分类；
3. 父分类和子分类是否必须具有相同用途；
4. 修改分类用途时，如何处理已经引用该分类的交易。

决策至少应明确分类用途枚举、合法父子组合、历史数据处理方式和报表聚合口径。

### DEC-REF-001：退款支持范围

**状态：已决定｜决定日期：2026-09-03｜关联 TODO：P2 退款与报表**

当前 MVP 只支持支出退款。收入退款、转账退款和冲正均不进入本次范围；出现明确产品场景后重新建项。

### DEC-REF-002：退款与冲正关系模型

**状态：已决定｜决定日期：2026-09-03｜关联 TODO：P2 退款与报表**

废弃 `is_refund` 与通用的 `related_transaction_id`。支出退款使用独立的
`expense_refund` 类型和 `refund_of_transaction_id`；冲正不进入当前 MVP，因此暂不增加
`reversal_of_transaction_id`。当前没有已发布数据库，不保留旧字段兼容层。

### DEC-REV-001：冲正定义

**状态：待决策｜关联 TODO：P1 核心账本规则**

需要决定：

1. 冲正是否必须完整抵消原交易且金额相等；
2. 每笔原交易是否最多存在一笔有效冲正；
3. 原交易已有部分退款时，允许冲正、拒绝冲正还是先处理退款；
4. 冲正交易能否再次成为退款或冲正的目标；
5. 冲正、作废和删除之间的产品边界。

### DEC-LIF-001：交易生命周期与入账语义

**状态：延后评估｜关联 TODO：延后评估**

当前个人 MVP 保留现有“有效 / 已作废”语义，不默认引入企业会计式完整入账状态。出现导入待确认、账单结算或不可变凭证需求时，再定义交易状态以及发生、录入、入账、修改、作废和冲正时间的关系。届时至少应明确：

- 哪些状态属于有效交易并影响余额；
- 每个状态允许修改的字段；
- 已入账交易是否禁止原地修改金额、类型、账户、分类和发生时间；
- 是否统一通过“冲正原交易并创建更正交易”修复账务错误；
- 描述、标签等非账务字段在入账后是否仍可修改。

### DEC-RPT-001：拆分交易与报表归属

**状态：待决策｜关联 TODO：P2 退款与报表**

当前 MVP 先实现收入、支出、退款抵减和转账排除的最小统计口径。以下能力延后决定：

1. 一笔交易能否拆分到多个分类；
2. 拆分金额之和与交易总额的精度和相等约束；
3. 分类树按本级、子树或两者分别聚合；
4. 退款、冲正、作废和更正交易在期间报表中的归属日期与抵减方式。

### DEC-AUTH-001：用户、所有权与数据隔离

**状态：MVP 后续｜关联 TODO：延后评估**

引入多用户能力前，需要定义账户、交易、分类和标签的所有者，以及跨用户引用、名称唯一性作用域和数据迁移规则。当前 MVP 不据此实现鉴权或资源隔离。

## 8. 当前实现差距

| 编号 | 差距 | 对应决策或 TODO |
| --- | --- | --- |
| GAP-001 | 交易创建/PATCH 契约尚未按类型表达可空账户和分类组合，与账本服务的路由规则不完全一致。 | 交易路由和分类约束 |
| GAP-002 | 服务层尚未统一验证账户组合、分类用途和 PATCH 合并后的完整状态；SQLite 不再负责这些跨表业务规则。 | 交易路由和分类约束 |
| GAP-003 | MVP 已决定保留分类树，但分类循环保护、分类用途和最小报表口径尚未完成。 | Issue #2、Issue #5、`DEC-CAT-001`、`DEC-RPT-001` |
| GAP-004 | 已决定使用 `expense_refund` 和 `refund_of_transaction_id`，但代码与契约仍保留旧字段，累计金额和报表规则尚未实现。 | Issue #10、`DEC-REF-001`、`DEC-REF-002` |
| GAP-005 | Python 服务和 SQLite 触发器同时维护余额投影，存在重复业务流程；目标是收敛到服务层维护。 | Issue #11 |
| GAP-006 | 后端当前仅注册健康检查路由，账本领域尚未形成可供客户端调用的完整 API。 | Issue #12、Issue #4 |
| GAP-007 | 运行时每个 SQLite 连接均启用外键约束的要求尚需纳入最小连接管理和验证。 | Issue #11 |
| GAP-008 | 版本化迁移、历史数据升级和回滚演练尚未建立，但当前没有已发布数据库，因此不阻塞 MVP 开发；Issue #9 已按“不计划”关闭。 | 延后评估 |
| GAP-009 | 基础资源删除尚未把外键冲突稳定映射为 `409 Conflict`，也没有对应 API 集成测试。 | Issue #12、`BR-RES-001` |

## 9. 决策记录模板

新增或关闭业务决策时使用以下格式：

```markdown
### DEC-XXX-NNN：决策标题

**状态：待决策 / 已决定｜决定日期：YYYY-MM-DD｜负责人：姓名或角色**

背景：说明为什么需要该决策。

决策：写明唯一、可执行的业务结论。

影响：列出 HTTP 契约、服务、数据模型、迁移、报表和测试影响。

验收：列出可以独立验证的结果。
```
