# PocketTally 待办事项

> [!NOTE]
> 本文件是待办事项的唯一清单。`TODO-IMP.md` 仅保留历史讨论和实施背景；同一问题以本文件的描述为准。
> 当前已确认规则、部分落地规则和待决策事项统一整理在 [`docs/business-rules.md`](docs/business-rules.md)；本文件只跟踪实施范围和完成状态。

## 当前 MVP 边界

- PocketTally 是个人使用、单用户、本地 SQLite 的小型记账应用。
- 只有 PocketTally 应用可以写入数据库；未来导入功能也必须经过同一业务服务。
- 手工 SQL 和第三方程序直接写数据库不属于受支持能力，不作为业务规则验收路径。
- SQLite 负责主键、外键、唯一性、非空、封闭枚举、正金额等结构约束；跨记录和跨表业务规则由服务层统一执行。
- v0.1.0 是首个已发布的 SQLite 数据库基线。当前没有版本化迁移；后续 Schema 变更前必须建立迁移，或明确兼容、备份与恢复策略。
- v0.1.0 只支持个人单用户、本地或可信内网使用；单用户公网鉴权由 Issue #22 负责，多用户数据隔离属于更远期范围。

## 已完成

- [x] 按 Issue #14 完成 Nuxt 工作台：接入真实资源、分页交易、退款摘要、六类服务端统计和消费下钻 API，支持核心记账、退款、编辑、作废、组合筛选和上海时区，并提供独立账本的浏览器端到端验收。
- [x] 按 Issue #20 建立 Chromium 桌面兼容性验收矩阵，固定覆盖 1280、1440 和 1920 三档宽度、五个主页面、关键弹窗、空状态、长文本、分页、多时间桶统计、明暗主题及错误输入保留；Firefox、WebKit、移动端专用矩阵和 CI 不属于当前阻断范围。
- [x] 建立适用于 macOS ARM64 与 Linux x86_64 的 Docker Compose 内网部署、双架构镜像流水线、SQLite 一致备份与停机恢复流程；当前无鉴权，只允许可信内网访问，不发布公网入口。
- [x] 按 Issue #23 同步 v0.1.0 发布文档、首个 SQLite 数据库基线、固定版本镜像部署、备份要求、鉴权规划边界和发行说明草稿。
- [x] 按 Issue #30 使用 Nuxt UI Dashboard、NavigationMenu、Breadcrumb、Tooltip 与 Slideover 重构统一应用壳层；宽屏支持可调整和折叠的侧栏，窄屏使用可访问的滑出导航，并以紧凑状态指示器保留账本、CNY 与 Asia/Shanghai 语义。
- [x] 按 Issue #31 将工作台迁移为六个 Nuxt 文件路由，区分总览与统计分析职责，并使用 URL query 保存交易和统计的用户可见筛选状态。
- [x] 按 Issue #26 将外观配置迁移到设置页，使用八套 registry 配色色板和 Nuxt UI 主题选项，并在侧边栏账本状态上方提供包含系统、明亮、暗色图标的三档滑动开关；移除顶栏重复入口并统一导航与主页面主题表面。
- [x] 按 Issue #40 统一六个业务页面的 `UMain`/`UContainer` 内容约束、页面流和响应式命名网格区域；保留分类树与统计可视化的业务专属交互，并完成三档 Chromium 布局验收。

- [x] 将当前已实现的基础交易类型限制为 `income`、`expense`、`transfer` 和 `balance_adjustment`，并要求交易金额为有限正数。
- [x] 要求创建交易提供 `occurred_at`，并在交易读取结果中返回；文档区分 `occurredAt`、`createdAt` 和 `updatedAt`。
- [x] 将交易标签从 `transactions.tags` JSON 字段规范化为 `transaction_tags` 关联表，使用复合主键和外键约束。
- [x] 在 ORM 与 SQLite DDL 中统一账户、分类和标签名称的 `NOCASE` 唯一性。
- [x] 在 ORM 和 SQLite DDL 中统一服务器默认值，并通过更新触发器维护 `updated_at`。
- [x] 禁止账户 PATCH 直接修改余额；余额调整使用 `balance_adjustment` 交易类型表达。
- [x] 确定账户类型为 `debit`、`credit`，并要求 `debit` 账户余额不得小于 0。
- [x] 确定余额以有效交易为权威来源，并实现服务层原子记账、交易修改、作废和余额重算。

## 下一步实施顺序

### P1：收敛当前实现

- [x] 按 Issue #11 移除 SQLite 跨表余额业务触发器，收敛到服务层维护 `accounts.amount_minor` 投影，并建立运行时数据库连接、请求级 Session 和连接级外键。
- [x] 保留树状分类，并在 Issue #12 前按 Issue #2 实现自身引用、多节点循环检测和异常祖先链的有限遍历保护。
- [x] 按 Issue #5 实现必填的 `income`/`expense` 分类用途、父子用途一致性、用途不可变和分类移动事务校验；含子树移动的用户确认由前端负责。
- [x] 按 Issue #12 实现账户、分类、标签 CRUD 与交易读取 API；仅允许物理删除未被引用的资源，被交易或子分类引用时返回 `409 Conflict`，不得级联删除交易或静默清空历史关系。
- [x] 按 Issue #4 实现统一交易创建、PATCH、幂等作废和余额调整写入，并同步标签关系、余额投影与审计读取规则。

### P1：核心账本规则

- [x] 完成 Issue #4 的交易写入 API 与路由组合：创建和 PATCH 接入同一服务层完整状态校验，SQLite 仅保留单行交易矩阵与作废状态约束。
- [x] 按 Issue #16 将退款与 Tag 聚合改为数据库侧有界 JOIN，补齐包含有效退款和作废退款的十万笔正确性回归与独立性能门禁；分页及全部统计/下钻接口均返回 `200`、金额一致，2026-09-09 实测最高 p95 为 0.693781 秒。
- [ ] 将未来导入入口接入同一业务服务；导入功能尚未实现前，不阻塞基础 CRUD 和 Issue #4 的应用写入路径验收。

### P2：退款与报表

- [x] 按 Issue #10 增加独立的 `expense_refund` 交易类型并实现支出退款；客户端只提交原支出、金额、发生时间和说明，账户与分类由服务端复制，普通交易写入口不得伪造退款关系。
- [x] 完成 `BR-RPT-002/003` 的报表口径：上海时区期间、退款双重归属、分类树递归归集、Tag 非互斥维度、整数分下钻和数据库侧有界聚合；拆分交易、冲正和更正仍延后决定。
- [x] 按 Issue #15 提供分页交易、退款摘要、六类统计与下钻接口；前端不下载全部交易自行聚合。
- [x] 按 Issue #19 将分类管理升级为用途 Tab、可展开多层分类树和分类详情检查器；支持子分类创建、移动确认、删除冲突、搜索祖先展开、异常链路保护及 ARIA 键盘访问。
- [x] 按 Issue #17 补齐前端纯键盘主流程、弹窗焦点约束与恢复、错误提示、重复提交和自动化无障碍验收；Chromium 矩阵覆盖创建账户/分类、调账、支出、详情、退款、作废、筛选和统计下钻。

### P1：v0.2 公网单用户发布

- [ ] 按 [Issue #22](https://github.com/LouisLiuNova/PocketTally/issues/22) 完成公网发布前的单用户鉴权安全边界；先形成威胁模型并决定技术方案，鉴权验收前不得部署公网 VPS、配置公网端口转发或把 Caddy/HTTPS 当作身份验证替代品。该 Issue 已挂入 [`v0.2 公网单用户发布`](https://github.com/LouisLiuNova/PocketTally/milestone/1) Milestone。
- [ ] Issue #22 验收通过后，再分别建立 VPS、Caddy、可信代理头与 HTTPS 的实施任务；本阶段不预选应用内登录、Caddy Basic Auth 或外部身份代理。

## 从当前设计中去除

- [x] 去除“任意直接 SQL 写入都必须满足完整业务规则”的目标和相应跨表触发器方案。
- [x] 去除 HTTP Schema、服务层、ORM 与 SQLite 重复实现全部业务规则的要求；各层只承担当前 MVP 边界中定义的职责。
- [x] 首次发布前未引入版本化迁移；Issue #9 已按“不计划”关闭。v0.1.0 起后续 Schema 变更必须重新建立迁移或明确兼容、备份与恢复策略。
- [x] 去除尚无实际需求依据的多用户隔离、高并发退款控制和企业级已入账不可变工作流。

## 保留的正确性要求

- 交易使用 CNY 整数分持久化，HTTP 金额通过 `Decimal` 和 `ROUND_HALF_UP` 转换。
- 新建账户余额为 0，账户余额不能通过账户 API 直接修改；调账必须创建 `balance_adjustment` 交易。
- 交易写入与余额投影更新必须在同一个 SQLite 事务中成功或回滚。
- 账户、分类和关联记录继续使用外键、唯一性及必要的单行 `CHECK`。
- `debit` 账户余额不得小于 0，转账必须使用两个不同账户且不计入收入或支出。
- 作废交易保留记录且不参与余额计算。

## 延后评估

- 版本化迁移与历史数据库升级：v0.1.0 已形成首个发布基线；后续 Schema 变更前按实际数据重新建项，不得假定数据库可以无迁移升级或只靠镜像回退。
- 单用户公网身份验证：由 Issue #22 在 v0.2 前定义威胁模型、身份验证、会话和公网安全边界；完成前仍只允许本地或可信内网使用。
- 多用户所有权与数据隔离：出现明确多用户需求时，再定义资源所有者、跨用户引用、名称唯一性作用域和数据迁移规则；不与 Issue #22 的单用户鉴权混写。
- 完整交易生命周期、冲正、更正链、拆分交易和复杂对账：出现明确产品场景后再设计。
- 第三方直接数据库写入、多进程写入或同步服务：一旦出现，应重新评估数据库层业务保护。

## 待确认的后续简化点

- 契约事实来源：当前同时维护 Pydantic、独立 JSON Schema、OpenAPI 和 Apidog bundle；实现真实 API 后，应单独决定是否改为由 FastAPI/Pydantic 生成契约并减少手工副本。
- 数据结构事实来源：当前同时维护 SQLModel、`schema.sql` 和 DBML；在决定数据库初始化方式前，不删除任何一份，但应避免继续要求三者重复表达跨表业务规则。
- 退款扩展：Issue #10 已确定使用独立的 `expense_refund` 类型实现支出退款；收入退款、转账退款、冲正和完整生命周期留到出现明确场景后重新建项。
- 资源归档：当前只允许物理删除未被引用的账户、分类和标签；出现隐藏历史资源的产品需求时，再单独设计 `archived_at` 或等价归档能力。

> [!IMPORTANT]
> 简化的是重复实现和当前没有证据支持的扩展能力，不是金额、余额、事务和审计正确性。
